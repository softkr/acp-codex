# ==================================================================
# ACP-Codex Bridge Environment Configuration
# ==================================================================
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env
# ==================================================================

# ------------------------------------------------------------------
# OPERATION MODE
# ------------------------------------------------------------------
# Set to "true" to use OpenAI Codex CLI (recommended)
# Set to "false" to use OpenAI API directly
USE_CODEX_CLI=true

# ------------------------------------------------------------------
# CODEX CLI CONFIGURATION (when USE_CODEX_CLI=true)
# ------------------------------------------------------------------
# Path to Codex CLI binary (optional, defaults to 'codex' in PATH)
# CODEX_CLI_PATH=/opt/homebrew/bin/codex

# ------------------------------------------------------------------
# OPENAI API CONFIGURATION (when USE_CODEX_CLI=false)
# ------------------------------------------------------------------
# OpenAI API Key - REQUIRED only when USE_CODEX_CLI=false
# Get your key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-api-key-here

# Model configuration for API mode
# CODEX_MODEL=gpt-5
# CODEX_TEMPERATURE=0.1
# CODEX_MAX_TOKENS=2000

# ------------------------------------------------------------------
# ACP BRIDGE CONFIGURATION
# ------------------------------------------------------------------
# Permission mode: default | acceptEdits | bypassPermissions
ACP_PERMISSION_MODE=acceptEdits

# Maximum conversation turns (0 = unlimited)
ACP_MAX_TURNS=100

# ------------------------------------------------------------------
# DEBUGGING & LOGGING
# ------------------------------------------------------------------
# Enable debug logging
# ACP_DEBUG=false

# Log to file (optional)
# ACP_LOG_FILE=./logs/acp-codex.log

# ------------------------------------------------------------------
# ADVANCED OPTIONS
# ------------------------------------------------------------------
# Custom path to Claude Code executable (for diagnostics)
# ACP_PATH_TO_CLAUDE_CODE_EXECUTABLE=/usr/local/bin/claude

# Node environment
# NODE_ENV=development

# Server port (if running as standalone server)
# PORT=3000
